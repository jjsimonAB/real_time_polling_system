const Config = {
    //replace pusherConfig with your pusher account config
    pusherConfig: {
        appId: '',
        key: '',
        secret: '',
        cluster: '',
        encrypted: true,
    },
    serverConfig: {
        PORT: 3000
    }

}

module.exports = Config;
